# Digit Templates

Optional OCR overrides live here.

If present, the solver will load `1.png` through `9.png` from this directory before falling back to generated templates. Each file should contain a single digit on a plain background.
